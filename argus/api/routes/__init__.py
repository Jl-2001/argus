"""Route modules -- one per resource, mounted by `argus.api.app.create_app`.

See that package's own docstring for the read-only/import boundary
every module here (except `doctor.py`, documented there) must hold to.
"""

from __future__ import annotations
